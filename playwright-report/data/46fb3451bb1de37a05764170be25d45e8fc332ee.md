# Instructions

- Following Playwright test failed.
- Explain why, be concise, respect Playwright best practices.
- Provide a snippet of code with the fix, if possible.

# Test info

- Name: guard.spec.ts >> Auth Guard >> should redirect unauthenticated users to sign-in
- Location: tests/e2e/guard.spec.ts:4:3

# Error details

```
Error: expect(page).toHaveURL(expected) failed

Expected pattern: /\/auth\/sign-in/
Received string:  "http://localhost:5173/dashboard"
Timeout: 5000ms

Call log:
  - Expect "toHaveURL" with timeout 5000ms
    9 × unexpected value "http://localhost:5173/dashboard"

```

# Page snapshot

```yaml
- generic [ref=e2]:
  - heading "404" [level=1] [ref=e3]
  - paragraph [ref=e4]: Not Found
```

# Test source

```ts
  1  | import { test, expect } from "@playwright/test";
  2  | 
  3  | test.describe("Auth Guard", () => {
  4  |   test("should redirect unauthenticated users to sign-in", async ({ page }) => {
  5  |     await page.goto("/dashboard");
> 6  |     await expect(page).toHaveURL(/\/auth\/sign-in/);
     |                        ^ Error: expect(page).toHaveURL(expected) failed
  7  |     await expect(page.getByRole("heading", { level: 1 })).toContainText("Sign in");
  8  |   });
  9  | 
  10 |   test("should redirect authenticated user without workspace to onboarding", async ({ page }) => {
  11 |     // Note: In a real CI pipeline, you'd seed a test user via API or test DB branch
  12 |     // This test validates the guard logic structure. For full coverage, pair with API seed.
  13 |     await page.goto("/onboarding");
  14 |     await expect(page.getByRole("heading", { level: 1 })).toContainText("Create your workspace");
  15 |   });
  16 | });
  17 | 
```